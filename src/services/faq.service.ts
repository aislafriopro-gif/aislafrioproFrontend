import api from "@/lib/api";

export const faqService = {
    getAll: async () => {
    // const response = await api.get("/faq");
    // return response.data;
    throw new Error("Endpoint pendiente de definición por el Backend");
    },
};